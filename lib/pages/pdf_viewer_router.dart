/*import 'package:flutter/foundation.dart' show kIsWeb;
import 'package:flutter/material.dart';
import 'pdf_viewer_page.dart';
import 'pdf_viewer_web.dart';


class PdfViewerRouter extends StatelessWidget {
  final String assetPath;

  const PdfViewerRouter({super.key, required this.assetPath});

  @override
  Widget build(BuildContext context) {
    if (kIsWeb) {
      return PdfViewerWeb(assetPath: assetPath);
    }
    return PdfViewerPage(assetPath: assetPath);
  }
}*/
